import Tooltip from './Tooltip.vue';

// 导入样式（自动引入）
import './tooltip.scss';

export type { TooltipProps, Position, Trigger, ArrowBounding, RenderContent, RenderContentProps } from './interface';

export default Tooltip;
export { Tooltip };
