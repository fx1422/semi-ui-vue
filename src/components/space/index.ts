import Space from './Space.vue';
import type { SpaceProps, Align, Spacing } from './interface';

// 导入样式（自动引入）
import './space.scss';

Space.name = 'SemiSpace';

export default Space;
export { Space };
export type { SpaceProps, Align, Spacing };
