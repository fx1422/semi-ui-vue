import Banner from './Banner.vue';
import type { BannerProps, BannerState, BannerType } from './interface';

// 导入样式（自动引入）
import './banner.scss';

export type { BannerProps, BannerState, BannerType };
export default Banner;
export { Banner };
