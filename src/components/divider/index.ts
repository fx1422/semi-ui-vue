import Divider from './Divider.vue';
import type { DividerProps } from './interface';

// 导入样式（自动引入）
import './divider.scss';

Divider.name = 'SemiDivider';

export default Divider;
export { Divider };
export type { DividerProps };
